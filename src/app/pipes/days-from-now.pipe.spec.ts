import { DaysFromNowPipe } from './days-from-now.pipe';

describe('DaysFromNowPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysFromNowPipe();
    expect(pipe).toBeTruthy();
  });
});
